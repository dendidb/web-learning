JQUery Add Elements
--


// menyisiokan content tag (html/plan text) ke dalama selector di bagian akhir sebelum close taag selector
selector.append(tagHTML);
selector.append(plainText);


// menyisiokan content tag (html/plan text) ke dalama selector di bagian aWAL sebelum close taag selector
selector.prepend(tagHTML);
selector.prepend(plainText);


// menyisiokan content tag (html/plan text) ke dalama selector di bagian aWAL sebelum close taag selector
selector.before(tagHTML);
selector.before(plainText);


// menyisiokan content tag (html/plan text) ke dalama selector di bagian aWAL sebelum close taag selector
selector.after(tagHTML);
selector.after(plainText);


JQUERY Remove
--


selector.remove();

selector.empty();

jQuery - Remove Attribute
// menghapus attribute

selector.removeAttr('attribute-name');
// delegate event 

$this



