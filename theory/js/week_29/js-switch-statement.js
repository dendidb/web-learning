JS Switch Statements
--

fungsinya untuk alternatif untuk cek nilai pengganti if

switch(inputDesa.value);


switch(expression) {
  case value {
    do
    break;
  case value;
    do
    break;
  }
}