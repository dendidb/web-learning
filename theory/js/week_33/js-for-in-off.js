JS For IN (return key)
--

var key (freetext)
for (key in object) {
  // code block to executed
}

JS For OF (return value); => perlu modern browser
-- 

for (varibles of iterable(array)) {
// code block to be executed
}






